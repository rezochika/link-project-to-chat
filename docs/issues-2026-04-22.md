# Project Issue Tracker — 2026-04-22

Audit of `lptc` codebase. 29 issues across 6 categories.

---

## CRITICAL

| # | File | Line | Issue |
|---|------|------|-------|
| C1 | `task_manager.py` | 329–336 | `subprocess.Popen(task.input, shell=True, ...)` — arbitrary shell command execution with no rate-limiting or resource cap specific to `/run` commands. Malicious user can spawn resource-exhausting processes. |

---

## HIGH

| # | File | Line | Issue |
|---|------|------|-------|
| H1 | `task_manager.py` | 241 | `raise RuntimeError(event.message)` — stream Error events raised without sanitization; sensitive info (API keys, paths) may leak into logs/user output. |
| H2 | `bot.py` | 1555 | Path traversal check uses `str.startswith()` instead of `Path.is_relative_to()`; symlinks and non-canonical paths can bypass it. |
| H3 | `claude_client.py` | 254–264 | `os.environ.copy()` passes ALL env vars (AWS_*, GITHUB_TOKEN, OPENAI_API_KEY…) to Claude subprocess; only `CLAUDECODE` and `CLAUDE_CODE_ENTRYPOINT` are stripped. |
| H4 | `botfather.py` | 110 | `chmod(0o600)` called **after** Telethon session file is populated; a race window exists where session credentials are world-readable. |
| H5 | tests/ | — | No tests for path traversal validation (`_send_image`). |
| H6 | tests/ | — | No tests verifying sensitive env vars are NOT forwarded to Claude subprocess. |

---

## MEDIUM

| # | File | Line | Issue |
|---|------|------|-------|
| M1 | `bot.py` | 557–562 | Race condition: task cancellation loop not synchronized; tasks may complete between `find_by_message` and cancel, leaving inconsistent state. |
| M2 | `claude_client.py` | 234–240 | `chat()` returns error strings prefixed `"Error:"` instead of raising; callers cannot distinguish real responses from errors. |
| M3 | `config.py` | 386–390 | `_patch_json`: non-`json.JSONDecodeError` exceptions leave lock file unreleased. |
| M4 | `config.py` | 228–230 | Lock file at predictable path `~/.link-project-to-chat/config.lock`; no randomization — susceptible to race/hijack on shared filesystems. |
| M5 | `config.py` | 293–348 | O(n) per-project lookup during config save; degrades for large project counts. |
| M6 | `task_manager.py` | 400–404 | `sorted(all_tasks)[: limit]` — sorts entire list every `/tasks` call; O(n log n) for long-running bots. |
| M7 | `livestream.py` | 108–109 | `_flush_soon` unconditionally creates new task on each completion; if deltas arrive faster than flushes, task queue grows unbounded. |
| M8 | `bot.py` | 1385 | Hardcoded `/tmp/link-project-to-chat`; use `tempfile.gettempdir()`. |
| M9 | `bot.py` | 1608–1609 | `delete_webhook(drop_pending_updates=True)` silently discards in-flight updates on restart with no log. |
| M10 | tests/ | — | Auth tests missing: concurrent attempt safety, rate-limit boundary (exactly 30 msg/min), multi-user mode transitions. |
| M11 | tests/ | — | Config I/O: no tests for malformed JSON, permission errors, concurrent access. |
| M12 | tests/ | — | `livestream._rotate_once`: no boundary/overflow tests. |
| M13 | docs / code | — | `_auth()` undocumented; no migration guide for single→multi-user auth transition. |

---

## LOW

| # | File | Line | Issue |
|---|------|------|-------|
| L1 | `config.py` | 180–182 | `print(..., file=sys.stderr)` instead of logger; inconsistent with rest of codebase. |
| L2 | `livestream.py` | 150–161 | Binary-search loop capped at 5 iterations; can still truncate at wrong boundary if HTML never fits. |
| L3 | `group_state.py` | 29–30 | `_states` dict grows without eviction; memory leak for bots with many unique chat IDs. |
| L4 | `livestream.py` | 18–20 | `_DEFAULT_THROTTLE`, `_DEFAULT_MAX_CHARS`, `_MAX_THROTTLE` are magic numbers; not configurable without code changes. |
| L5 | `_auth.py` | 73 | Username compared `.lower()` but not `.strip()`; leading/trailing whitespace could theoretically bypass allowlist. |
| L6 | `task_manager.py` | 304–311 | `COMPACT_PROMPT` constant undocumented. |
| L7 | various | — | Missing versioning/changelog; no notes on breaking auth refactor. |

---

## Fix Priority Order

1. **C1** — `/run` resource exhaustion (no rate-limit on spawned processes)
2. **H2** — Path traversal via `str.startswith` → `Path.is_relative_to`
3. **H3** — Env var leakage to Claude subprocess
4. **H4** — Session file chmod race
5. **H1** — Unsanitized error messages
6. **H5/H6** — Missing security tests
7. **M1–M13** — Medium issues (see table)
8. **L1–L7** — Low issues

---

_Generated by PM audit — assign fixes to dev bot per priority._
