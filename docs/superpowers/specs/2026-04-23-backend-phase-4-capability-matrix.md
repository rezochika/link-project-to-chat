# Backend Phase 4 Capability Matrix

**Captured:** 2026-04-26 from automated tests at HEAD `9280aef` plus one direct Codex CLI smoke run (thread `019dc8db-0ea5-7193-8f58-d28059401947`).

## Re-check log

- 2026-04-26 (afternoon): matrix re-validated against the same test/smoke evidence at unchanged HEAD `03ef9e0`. `tests/backends/test_capability_declaration.py::test_codex_capabilities_match_validated_findings` still pins every `False`/`()` Codex flag. Live smoke (thread `019dc923-7953-7be3-a7cc-3706085fbc7d`) reproduced the same event sequence (`thread.started` / `turn.started` / `item.completed (agent_message)` / `turn.completed`) — no thinking event, no rate-limit signal, no new model identifier on stdout. No capabilities flipped; no matrix update needed.

| Capability | Claude | Codex | Evidence |
|------------|--------|-------|----------|
| Resume/session reuse | yes | yes | Claude: `tests/test_stream.py::test_result_event` proves `session_id` round-trips through `parse_stream_line`; `src/link_project_to_chat/backends/claude.py:255-256` shows `--resume <session_id>` is wired into `_build_cmd`. Codex: `tests/backends/test_codex_live.py::test_codex_live_resume_reuses_session` asserts `backend.session_id` is unchanged after a second `chat()`; bash smoke run on 2026-04-26 saw the resume turn echo the original `thread_id`. |
| Live text streaming | yes | yes | Claude: `tests/test_bot_streaming.py::test_text_delta_starts_live_message`, `::test_finalize_with_live_text_does_not_resend`, etc. — 22 streaming tests pass. Codex: `tests/backends/test_codex_backend.py::test_chat_stream_emits_text_delta_then_result` (fixture-based) and `tests/backends/test_codex_live.py::test_codex_live_round_trip` (live CLI) both assert `TextDelta` events arrive before the closing `Result`. |
| Thinking stream | yes | no | Claude: `tests/test_bot_streaming.py::test_thinking_delta_with_toggle_on_streams_separate_message`, `::test_thinking_delta_with_toggle_off_uses_buffer`, `::test_thinking_delta_in_group_mode_uses_buffer_not_livestream` — `ThinkingDelta` event is observed and routed through the bot. Codex: `CODEX_CAPABILITIES.supports_thinking = False` (`backends/codex.py:24`); pinned by `tests/backends/test_capability_declaration.py`; no thinking event observed on stdout in the bash smoke run (only `thread.started` / `turn.started` / `item.completed (agent_message)` / `turn.completed`); `codex --help` exposes no `--thinking` flag (`docs/superpowers/specs/2026-04-23-codex-cli-findings.md:41`). |
| Model selection via command | yes | no | Claude: `src/link_project_to_chat/backends/claude.py:82` enumerates `MODELS = ("haiku", "sonnet", "opus", "opus[1m]", "sonnet[1m]")`; `tests/test_capability_gating.py::test_model_command_rejected_when_backend_has_no_models` proves the `/model` gate flips on `capabilities.models`. Codex: `CODEX_CAPABILITIES.models = ()` (`backends/codex.py:23`); `tests/test_capability_gating.py::test_model_command_rejected_when_backend_has_no_models` (parametrized over the codex backend) asserts `/model` is rejected; the CLI accepts `--model <name>` but advertises no validated list (`docs/superpowers/specs/2026-04-23-codex-cli-findings.md:43, 66`). |
| Permission switching via command | yes | no | Claude: `PERMISSION_MODES = ("default", "acceptEdits", "bypassPermissions", "dontAsk", "plan", "auto")` at `backends/claude.py:83`; `capabilities.supports_permissions = True` at `claude.py:168`. Codex: `supports_permissions = False` (`backends/codex.py:25`); `tests/test_capability_gating.py::test_permissions_command_rejected_when_backend_does_not_support_it` asserts `/permissions` is rejected; the CLI exposes no `--permission` flag in the non-interactive surface (`codex-cli-findings.md:41, 63`). |
| Compact/session compression | yes | no | Claude: `capabilities.supports_compact = True` at `claude.py:170`; the bot's `/compact` command path is gated through this flag. Codex: `supports_compact = False` (`backends/codex.py:27`); `tests/test_capability_gating.py::test_compact_command_rejected_when_backend_does_not_support_it` asserts `/compact` is rejected; `codex` CLI exposes no compact / session-management subcommand (`codex-cli-findings.md:41, 64`). |
| Usage-cap detection | yes | no | Claude: `_USAGE_CAP_PATTERNS` at `backends/claude.py:42-47` plus `_detect_usage_cap` and `is_usage_cap_error` (`claude.py:50-67`); `capabilities.supports_usage_cap_detection = True` at `claude.py:172`; `tests/test_claude_usage_cap.py` exercises the matcher. Codex: `supports_usage_cap_detection = False` (`backends/codex.py:29`); the bash smoke run produced only token counts in `turn.completed.usage` (`input_tokens`, `cached_input_tokens`, `output_tokens`, `reasoning_output_tokens`); no rate-limit signal observed (`codex-cli-findings.md:48-56, 65`). |
| Provider-specific `/status` details | partial | partial | Both: `bot.py:_compose_status` (`src/link_project_to_chat/bot.py:1842-1865`) reports Backend, Model (via `model_display or model`), Session, Agent (running/idle), Running/Waiting tasks, Skill, Persona. Neither backend's `/status` exposes token usage (`CodexBackend.status` returns `last_usage` at `backends/codex.py:225` but `_compose_status` never reads it; `ClaudeBackend.status` returns `total_requests` / `last_duration` at `backends/claude.py:434-436` but `_compose_status` never reads them). Neither exposes rate-limit state. Phase 4 §2.2 calls out exactly this gap. |
