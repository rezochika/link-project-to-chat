# Backend Phase 4 Capability Matrix

**Captured:** 2026-04-26 from automated tests at HEAD `9280aef` plus one direct Codex CLI smoke run (thread `019dc8db-0ea5-7193-8f58-d28059401947`).

## Re-check log

- 2026-04-26 (afternoon): matrix re-validated against the same test/smoke evidence at unchanged HEAD `03ef9e0`. `tests/backends/test_capability_declaration.py::test_codex_capabilities_match_validated_findings` still pins every `False`/`()` Codex flag. Live smoke (thread `019dc923-7953-7be3-a7cc-3706085fbc7d`) reproduced the same event sequence (`thread.started` / `turn.started` / `item.completed (agent_message)` / `turn.completed`) — no thinking event, no rate-limit signal, no new model identifier on stdout. No capabilities flipped; no matrix update needed.
- 2026-04-26 (late afternoon): matrix updated for the **first capability-expansion slice** (`93f8b9c` Codex `/model` + `/effort`; `e2e2143` `/backend` button picker). Two cells flipped from `no` to `yes` for Codex (Model selection via command, Reasoning effort selection — new row). One existing cell (UX of `/backend` itself) is now also picker-driven.
- 2026-04-26 (evening): matrix updated for Codex `/permissions` (`2b1dba6`) and provider-aware `/status` completion. Codex permission modes now map to sandbox/approval flags. `/status` reports backend permission state, Claude tool allow/deny lists, request/duration/token usage, usage-cap state where detectable, and last backend error.

| Capability | Claude | Codex | Evidence |
|------------|--------|-------|----------|
| Resume/session reuse | yes | yes | Claude: `tests/test_stream.py::test_result_event` proves `session_id` round-trips through `parse_stream_line`; `src/link_project_to_chat/backends/claude.py:255-256` shows `--resume <session_id>` is wired into `_build_cmd`. Codex: `tests/backends/test_codex_live.py::test_codex_live_resume_reuses_session` asserts `backend.session_id` is unchanged after a second `chat()`; bash smoke run on 2026-04-26 saw the resume turn echo the original `thread_id`. |
| Live text streaming | yes | yes | Claude: `tests/test_bot_streaming.py::test_text_delta_starts_live_message`, `::test_finalize_with_live_text_does_not_resend`, etc. — 22 streaming tests pass. Codex: `tests/backends/test_codex_backend.py::test_chat_stream_emits_text_delta_then_result` (fixture-based) and `tests/backends/test_codex_live.py::test_codex_live_round_trip` (live CLI) both assert `TextDelta` events arrive before the closing `Result`. |
| Thinking stream | yes | no | Claude: `tests/test_bot_streaming.py::test_thinking_delta_with_toggle_on_streams_separate_message`, `::test_thinking_delta_with_toggle_off_uses_buffer`, `::test_thinking_delta_in_group_mode_uses_buffer_not_livestream` — `ThinkingDelta` event is observed and routed through the bot. Codex: `CODEX_CAPABILITIES.supports_thinking = False` (`backends/codex.py:29`); pinned by `tests/backends/test_capability_declaration.py`; no thinking event observed on stdout in the bash smoke run (only `thread.started` / `turn.started` / `item.completed (agent_message)` / `turn.completed`); `codex --help` exposes no `--thinking` flag (`docs/superpowers/specs/2026-04-23-codex-cli-findings.md:41`). |
| Model selection via command | yes | yes | Claude: `ClaudeBackend.MODEL_OPTIONS` enumerates 5 entries; `tests/test_capability_gating.py::test_model_command_rejected_when_backend_has_no_models` proves the `/model` gate flips on `capabilities.models`. Codex (since `93f8b9c`): `CODEX_CAPABILITIES.models = ("gpt-5.5", "gpt-5.4", "gpt-5.4-mini", "gpt-5.3-codex", "gpt-5.2")` (`backends/codex.py:25, 28`); `CodexBackend.MODEL_OPTIONS` populates the picker with priority-ordered display names from `~/.codex/models_cache.json`. `tests/test_backend_command.py::test_codex_model_command_shows_picker_for_codex` asserts the 5-button picker; `tests/backends/test_codex_backend.py::test_build_cmd_combines_model_and_effort` asserts the resulting `--model <slug>` arg. |
| Reasoning effort selection | yes | yes | Both (since `93f8b9c`): `BackendCapabilities.supports_effort` and `effort_levels` declared per backend; `effort` is now an `AgentBackend` Protocol attribute (was Claude-only tier-2). Claude: `effort_levels = ("low", "medium", "high", "xhigh", "max")`. Codex: `effort_levels = ("low", "medium", "high", "xhigh")` (matches `~/.codex/models_cache.json` `supported_reasoning_levels`); the per-backend `effort_levels` design avoids leaking Claude's `max` into the Codex picker. `tests/test_backend_command.py::test_codex_effort_command_shows_picker_for_codex` covers the picker; 4 `tests/backends/test_codex_backend.py::test_build_cmd_*` permutations cover the `-c model_reasoning_effort=<level>` arg construction (effort-only, no-effort, model+effort, resume+effort). |
| Permission switching via command | yes | yes | Claude: `current_permission()` / `set_permission()` wrap `skip_permissions` + `permission_mode`, and `capabilities.supports_permissions = True`. Codex (since `2b1dba6`): `CODEX_CAPABILITIES.supports_permissions = True`; `CodexBackend._permission_args()` maps `plan` to read-only sandbox + never approval, `acceptEdits`/`dontAsk`/`auto` to `--full-auto`, and bypass modes to `--dangerously-bypass-approvals-and-sandbox`. Covered by `tests/backends/test_codex_backend.py::test_build_cmd_includes_read_only_sandbox_for_plan_permissions`, `::test_build_cmd_includes_full_auto_for_auto_permissions`, `::test_build_cmd_includes_dangerous_bypass_for_skip_permissions`, and `tests/test_capability_gating.py::test_permissions_button_updates_active_backend_permissions`. |
| Compact/session compression | yes | no | Claude: `capabilities.supports_compact = True`; the bot's `/compact` command path is gated through this flag. Codex: `supports_compact = False` (`backends/codex.py:32`); `tests/test_capability_gating.py::test_compact_command_rejected_when_backend_does_not_support_it` asserts `/compact` is rejected; `codex` CLI exposes no compact / session-management subcommand (`codex-cli-findings.md:41, 64`). |
| Usage-cap detection | yes | no | Claude: `_USAGE_CAP_PATTERNS` at `backends/claude.py:42-47` plus `_detect_usage_cap` and `is_usage_cap_error` (`claude.py:50-67`); `capabilities.supports_usage_cap_detection = True`; `tests/test_claude_usage_cap.py` exercises the matcher. Codex: `supports_usage_cap_detection = False` (`backends/codex.py:34`); the bash smoke run produced only token counts in `turn.completed.usage` (`input_tokens`, `cached_input_tokens`, `output_tokens`, `reasoning_output_tokens`); no rate-limit signal observed (`codex-cli-findings.md:48-56, 65`). |
| Backend selection UX | partial | partial | Both (since `e2e2143`): `/backend` (no args) renders a button picker — one row per registered backend, active marked with ●. `tests/test_backend_command.py::test_backend_command_renders_picker_with_active_marker` covers initial render; `::test_backend_button_click_switches_backend` covers click-to-switch with in-place message edit + picker re-render. Typed `/backend <name>` still works for muscle-memory and tests; the four-form switch logic (no-op / unknown / live-task / activate-first persist) is shared via `_switch_backend`. |
| Provider-specific `/status` details | yes | yes | Both: `_compose_status` reports backend, friendly model label, effort, permissions, session/task state, request count, last duration, last error, skill, and persona. Claude additionally reports allowed/disallowed tools and usage-cap state; Codex reports last token usage from `turn.completed.usage`. Covered by `tests/test_backend_command.py::test_status_surfaces_codex_last_usage_tokens`, `::test_status_surfaces_claude_last_duration`, `::test_status_includes_permissions_for_supporting_backend`, `::test_status_includes_claude_allowed_and_disallowed_tools`, `::test_status_surfaces_usage_cap_flag_for_claude`, `::test_status_surfaces_last_backend_error`, plus backend status tests. |
